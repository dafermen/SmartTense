# Test Organization

Existing tests stay at `tests/*.test.js`. Place new coverage in `unit/`, `integration/`, `contract/`, or `e2e/`; place deterministic data in `fixtures/`. Do not move tests only for cosmetic organization.

Property, mutation, fuzzing, security, resilience, and performance governance lives in `docs/TESTING.md`.
